import { SpinnerShowDirective } from './spinner-show.directive';

describe('SpinnerShowDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinnerShowDirective(null, null, null);
    expect(directive).toBeTruthy();
  });
});
